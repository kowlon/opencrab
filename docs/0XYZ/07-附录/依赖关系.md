# 依赖关系

## pyproject.toml 主要依赖

### 核心依赖

| 包 | 版本 | 说明 |
|------|------|------|
| `anthropic` | * | Anthropic API 客户端 |
| `openai` | * | OpenAI API 客户端 |
| `mcp` | >=1.0.0 | Model Context Protocol |
| `fastapi` | * | Web 框架 |
| `uvicorn` | * | ASGI 服务器 |
| `aiosqlite` | * | 异步 SQLite |
| `httpx` | * | HTTP 客户端 |
| `aiofiles` | * | 异步文件操作 |

### 工具依赖

| 包 | 版本 | 说明 |
|------|------|------|
| `ddgs` | * | DuckDuckGo 搜索 |
| `playwright` | * | 浏览器自动化 |
| `browser-use` | * | 浏览器使用框架 |
| `pillow` | * | 图片处理 |

### CLI/UI 依赖

| 包 | 版本 | 说明 |
|------|------|------|
| `rich` | * | 终端富文本 |
| `prompt-toolkit` | * | 交互式 prompt |
| `typer` | * | CLI 框架 |

### IM 渠道依赖

| 包 | 版本 | 渠道 |
|------|------|------|
| `python-telegram-bot` | * | Telegram |
| `lark-oapi` | * | 飞书 |
| `dingtalk-stream` | * | 钉钉 |
| `wechatpy` | * | 企业微信 |

## 可选依赖

```toml
[project.optional-dependencies]
feishu = ["lark-oapi"]
dingtalk = ["dingtalk-stream"]
wework = ["wechatpy"]
onebot = ["nonebot2"]
qqbot = ["aiocqhttp"]
dev = ["pytest", "ruff", "mypy"]
```

## 开发依赖

| 包 | 版本 | 说明 |
|------|------|------|
| `pytest` | * | 测试框架 |
| `pytest-asyncio` | * | 异步测试 |
| `ruff` | * | Linter + 格式化 |
| `mypy` | * | 类型检查 |
| `pytest-cov` | * | 覆盖率 |

## 相关链接

- 上一页：[配置文件说明](./配置文件说明.md)
- 下一页：[变更记录](./变更记录.md)
